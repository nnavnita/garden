# bullseye.yaml — convergence targets for this project.
#
# Targets are desired project states expressed as testable properties,
# with dependency tracking and frontier computation. This file is the
# source of truth: read and written by the bullseye MCP server, and safe
# to hand-edit (the targets live below).
#
# What is bullseye / how do I install it?
#   https://github.com/marcelocantos/bullseye
#
# This banner is regenerated by bullseye on every write — edit the
# targets, not these lines.
schema_version: 5
targets:
  T1:
    name: Personal Obsidian vault publishes cleanly as a Quartz digital garden on every push to main
    status: achieved
    value: 5.0
    cost: 3.0
    acceptance:
    - '`.github/workflows/deploy.yml` builds (`npx quartz build`) and deploys to GitHub Pages on push to main'
    - '`content/` (the Obsidian vault) renders through quartz.config.yaml''s configured plugins without build errors'
    - '`npm run check` (tsc + prettier) passes'
    context: 'Fork of jackyzha0/quartz v5 with origin=nnavnita/garden, upstream=jackyzha0/quartz. The upstream repo''s CI/preview/docker workflows are gated to only run on jackyzha0/quartz (`if: github.repository == ''jackyzha0/quartz''`) so they''re inert here; only deploy.yml is live for this fork. Next real target is filling out content/, not infrastructure.'
    tags:
    - site
    - notes
    origin: bullseye_init
    discovered: 2026-08-15
    achieved: 2026-08-15
